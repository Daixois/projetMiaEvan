{% extends 'base.html.twig' %}

{% block title %}Hello MemoryController!{% endblock %}

{% block body %}

<div class="grille">
    {%for personnages in theme.characters%}

        <div class="carte"  data-attr="{{personnages.id}}">
                <div class="double-face">
                    <div class="face">
                        <img src="{{vich_uploader_asset(personnages, 'imageFile')}}">
                    </div>
            
                <div class="arriere">
                        <span>❓</span>
                </div>
        </div>
    
        <div class="carte"  data-attr="{{personnages.id}}">
                <div class="double-face">
                    <div class="face">
                        <img src="{{vich_uploader_asset(personnages, 'imageFile')}}">
                    </div>
            
                <div class="arriere">
                        <span>❓</span>
                </div>
        </div>

    {%endfor%}

     <script src="{{asset('js/app.js')}}"></script>
     
</div>
{% endblock %}
